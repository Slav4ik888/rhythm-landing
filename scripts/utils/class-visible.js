
/**
 * add not-visible class to element
 * @param {element} el 
 */
export const addNotVisible = (el) => el.classList.add('not-visible');

/**
 * remove not-visible class to element
 * @param {element} el 
 */
export const removeNotVisible = (el) => el.classList.remove('not-visible');
